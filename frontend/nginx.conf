server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA-Fallback: alle Routen an index.html, damit vue-router (history mode) funktioniert.
  location / {
    try_files $uri $uri/ /index.html;
  }
}